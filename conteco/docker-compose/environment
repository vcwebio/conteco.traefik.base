#!/usr/bin/env bash

# START CONFIGURATION
DOLLAR='$'
export CONTECO_DC_GLOBAL_STACKS='traefik.base'
export CONTECO_DC_GLOBAL_STACKNAMES="traefik"
export CONTECO_DC_GLOBAL_VOLUME_LIST="traefik_volume"
export CONTECO_DC_GLOBAL='$CONTECO_DC_GLOBAL_SERVICE_NAME,$CONTECO_DC_GLOBAL_IMAGE_NAME,$CONTECO_DC_TRAEFIK_ENVIRONMENT,$CONTECO_DC_TRAEFIK_COMMAND,$CONTECO_DC_TRAEFIK_DEPLOY_MODE,$CONTECO_DC_TRAEFIK_NETWORKS,$CONTECO_DC_GLOBAL_NETWORKS'
# END CONFIGURATION

# START GLOBAL SETTINGS
export CONTECO_DC_GLOBAL_SERVICE_NAME="${CONTECO_TYPE}"
export CONTECO_DC_GLOBAL_IMAGE_NAME="${CONTECO_TYPE}.${CONTECO_NAME}:${CONTECO_TAG}"
export CONTECO_DC_TRAEFIK_ENVIRONMENT=
export CONTECO_DC_TRAEFIK_COMMAND=
export CONTECO_DC_TRAEFIK_DEPLOY_MODE="replicated"
export CONTECO_DC_TRAEFIK_NETWORKS="conteco_overlay"
export CONTECO_DC_TRAEFIK_DEPLOY_LABELS=
export CONTECO_DC_GLOBAL_NETWORKS="conteco_overlay: { external: true }"
# END GLOBAL SETTINGS

# START SETTINGS nginx.base
export CONTECO_DC_TRAEFIK_PORTS="80:80,8080:8080"
export CONTECO_DC_TRAEFIK_VOLUMES="/var/run/docker.sock:/var/run/docker.sock"
export CONTECO_DC_TRAEFIK_DEPLOY_RESOURCES_LIMITS_CPUS="0.75"
export CONTECO_DC_TRAEFIK_DEPLOY_RESOURCES_RESERVATIONS_CPUS="0.1"
export CONTECO_DC_TRAEFIK_DEPLOY_RESOURCES_LIMITS_MEMORY="128m"
export CONTECO_DC_TRAEFIK_DEPLOY_RESOURCES_RESERVATIONS_MEMORY="32m"
# END SETTINGS nginx.base

# START TOPLEVEL SETTINGS
export CONTECO_DC_GLOBAL_VOLUMES=
# END TOPLEVEL SETTINGS
